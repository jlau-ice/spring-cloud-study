spring:
  datasource:
    url: jdbc:mysql://${ice.db.host:127.0.0.1}:${ice.db.port:3308}/${ice.db.database}?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${ice.db.un:root}
    password: ${ice.db.pw:sxkj0818web}
mybatis-plus:
  configuration:
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
  global-config:
    db-config:
      update-strategy: not_null
      id-type: auto